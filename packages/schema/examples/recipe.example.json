{
  "id": "pc.list-page.basic",
  "platform": "pc",
  "intent": "list-page",
  "layout": "page-shell",
  "requiredBlocks": ["neutral.filter-bar"],
  "optionalBlocks": [],
  "regions": [
    {
      "name": "body",
      "accepts": ["neutral.filter-bar"],
      "required": true
    }
  ],
  "layoutRules": ["Render neutral.filter-bar at the top of the body region before app-specific results."],
  "interactionRules": ["Swap the app-specific result region for empty state when data is absent."],
  "recommendedTokens": ["semantic.color.action.primary"],
  "antiPatterns": ["Do not place destructive bulk actions above filters."]
}
